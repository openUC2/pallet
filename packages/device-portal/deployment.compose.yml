services:
  server:
    image: ghcr.io/openuc2/device-portal:0.1.1-alpha.1
    environment:
      - TEMPLATES_PATH=/web/templates
    volumes:
      - type: bind
        source: ./templates
        target: /web/templates
        read_only: true
networks:
  default:
    name: none
    external: true
